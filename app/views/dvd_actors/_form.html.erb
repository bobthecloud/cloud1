<%= form_for(@dvd_actor) do |f| %>
  <% if @dvd_actor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dvd_actor.errors.count, "error") %> prohibited this dvd_actor from being saved:</h2>

      <ul>
      <% @dvd_actor.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :dvd_id %><br />
    <%= f.number_field :dvd_id %>
  </div>
  <div class="field">
    <%= f.label :actor_id %><br />
    <%= f.number_field :actor_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
